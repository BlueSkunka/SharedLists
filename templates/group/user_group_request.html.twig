{{ form_start(form) }}
{{ form_errors(form) }}
<div class="row g-3 align-items-center">
	<div class="col-auto">
		{{ form_widget(form.receiver) }}
	</div>
	<div class="col-auto">
		<button type="submit" class="btn btn-sm btn-primary">Ajouter un ami</button>
	</div>
</div>
{{ form_end(form) }}
