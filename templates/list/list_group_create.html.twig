{{ form_start(form) }}
{{ form_errors(form) }}

<h2>Créer une liste</h2>
<div class="row">
	<div class="row g-3 align-items-center">
		<div class="col-auto">
			{{ form_widget(form.name) }}
		</div>
		<div class="col-auto">
			<button type="submit" class="btn btn-sm btn-success">Créer une liste</button>
		</div>
	</div>
</div>

{{ form_end(form) }}
