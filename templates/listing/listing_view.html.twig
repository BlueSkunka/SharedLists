{% if listing is not null %}
    {% for item in listing.listingItems %}
        {{ item.name }} {{ item.reference != '' and item.reference is not null ? ' - ' ~ item.reference : '' }} <br>
    {% endfor %}
{% else %}
    Ce membre ne possède pas de liste. 
{% endif %}